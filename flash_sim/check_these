functions to add:
1. partial page update by write, need to generate a related read transaction and merge bitmap of update requests within the same page
2. gc unit
3. check multi-queue arrangement
4. add static write support and read support for static region
5. static region configuration

block_keeping_book construction should be modified, together with page metadata

gc_barrier 目前有问题，会在决定了gc_read的page之后超gc_target_block中新进行写入，这些page在后续erase的时候会丢失数据，需要阻止这种行为。
目前出现这个问题的原因是所有barrier都在Submit_trans之前执行判断，需要修改为在Submit_trans之后执行判断。
但直接这样改也会有其它的问题，比如如果先提交了一个write再提交了一个read，那么read会因为barrier一直无法执行，因此系统会一直尝试激活read对应的chip，导致系统卡死。
为了避免这种情况，就需要令issue_command也返回一个有没有成功dispatch的bool值，如果成功dispatch了，则直接返回True，否则返回False。如果返回了False，需要顺序尝试后续的激活操作

下一步：加入invalidate write操作（在连续写一个lpa的时候目前没法将）