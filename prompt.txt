当前的代码输出是比较naive的文本信息，我需要把它做成一个可视化的版本，用泳道图展示指令的执行过程，具体要求如下：
1. 泳道的横轴为仿真时间轴
2. 泳道的时间轴支持滚动、缩放查看
3. 泳道分为两个大的组别，分别显示request执行进程、transaction执行进程，下面分别描述每个组的功能要求。

## request组别
1. 该组泳道按照stream_id划分，每个stream_id显示一条泳道。要求泳道的stream_id在
2. req在host执行REQ_INIT事件的时候开始用色块显示在泳道中，直到host执行到这条req的REQ_COMP时结束显示
3. 一条req在host端会经历REQ_INIT, DELIVER, *_DATA（如果是write, compute, search, static_write请求），和最后的REQ_COMP这些事件，分别标志着其压入SQ队列、发送到Device、发送data到Device和完成，
要求将这些阶段用不同的颜色标出
4. 每条req在显示的时候展示它的type，start_lha, size

## transaction组别
1. transaction进程的泳道数量为channel_num * chip_num，先分成channel_num组，每组再分出chip_num个泳道。要求可视化的时候泳道的channel_id和chip_id保持显示在每条泳道的最左端
2. transaction在其被提交至chip_queue的时候开始显示在对应chip的泳道中，当PHY模块执行完其对应的COMPLETE（如果是WRITE）/DATA_TRANSFERED（如果是READ, COMPUTE, SEARCH）之后结束显示
3. transaction开始显示之后会经历dispatch, CMP_TRANSFERED, COMPLETE, DATA_TRANSFERED(if any)等事件，每经历一个事件需要改变色块的颜色以标记不同阶段
4. 每条transaction在显示的时候展示其type, source_req（如果是GC或者MAPPING显示None即可）, accessed_lpa, accessed_address等信息


请根据上述需求，选择合适的工具完成可视化代码。

你的权限：
1. flash_sim目录中读取相关的代码，并可以在这些文档中增加代码
2. ！！禁止修改Host.py, HIL.py, FTL.py, PHY.py中现有的代码！！避免出现仿真器核心功能错误！！！
3. 我创建了一个conda环境，名为flash-sim，已经安装了python=3.12以及pip工具。你可以在这个环境中安装你需要的python库