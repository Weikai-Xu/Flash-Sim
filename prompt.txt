帮我把common.py文件中跟flash硬件配置有关的内容加入到config.py文件中的FlashGeometry类中


请帮我根据parser.COMMAND_SCHEMA生成一个test_trace.json文件，在time为0的时刻生成read, write, search, compute操作各一个
start_lha和size随机。
需要注意search和compute的操作start_lha需要大于FlashGeometry.static_area_base_address，并且满足要求的lha将对应一个sub_plane

在engine.py文件中的Initialize_event_queue()函数中，调用parser.py中的函数，将test_trace.json文件中的req利用Register_sim_event()压入
event_queue中，req放在event.param字段上